---
- name: Ensure firewall init script exists
  copy: src=rc.firewall dest=/etc/rc.d/rc.firewall

- name: Ensure IPTables config file is in place
  copy: src=iptables.conf dest=/etc/iptables.conf owner=root group=root mode=0644
